[project]
name = "myapi"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "sqlalchemy>=2.0.0",
  "pydantic>=2.7.0",
  "pytest>=8.0.0",
]

[tool.ruff]
line-length = 88

[tool.black]
line-length = 88
